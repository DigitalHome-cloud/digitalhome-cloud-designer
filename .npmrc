# Match the dependency-resolution permissiveness of `yarn install` (the
# tool we use locally). gatsby-plugin-react-i18next@3.0.1 declares a peer
# i18next@^22, but the repo runs against i18next@25 — yarn ignores the
# range, npm v7+ refuses it without this flag.
legacy-peer-deps=true
